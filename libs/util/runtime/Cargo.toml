[package]
name = "runtime"
description = "A wrapper around BevyECS that enable it to be used as the memory for a scripting engine."
version = "0.0.0"
edition = "2021"

[dependencies]
anyhow = "^ 1"
bevy_ecs = { version = "^ 0.6", features = ["trace"] }
bevy_tasks = "^ 0.6"
itertools = "^ 0.10"
log = "^ 0.4"
pretty-type-name = "1.0"
structopt = "^ 0.3"
nitrous = { path = "../../util/nitrous" }
